{
  "name": "lodge804site",
  "version": "1.0.0",
  "private": true,
  "description": "Alpha Xi — Static site with npm dev/start scripts",
  "scripts": {
    "dev": "live-server --port=8000 --open=.",
    "start": "http-server ./ -p 8000",
    "build": "echo \"No build step for static site\""
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "live-server": "^1.2.0"
  }
}
